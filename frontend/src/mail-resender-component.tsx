import React from "react";

interface Props {
   name: string;
}

const Mail-resenderComponent = (props: Props) => {
   return (
      <div>
         Hello {props.name}
      </div>
   );
}

export default Mail-resenderComponent;